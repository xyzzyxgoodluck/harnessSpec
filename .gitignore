# 构建产物
target/
*.jar
*.class

# IDE
.idea/
*.iml
.vscode/

# 本地环境与密钥
.env
*.log
.DS_Store
Thumbs.db

# 临时目录与临时产物（脚本生成的中间物；`.tmp-*` 同时匹配目录与文件——
# 只写 `.tmp-*/` 时文件不会被忽略，实测 `.tmp-audit-check.txt` 仍会进 git）
.tmp-*
